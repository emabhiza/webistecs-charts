apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.elasticsearch.name }}-service
  namespace: {{ .Values.elasticsearch.namespace }}
  labels:
    app: {{ .Values.elasticsearch.name }}
spec:
  type: {{ .Values.elasticsearch.service.type }}
  ports:
    - port: {{ .Values.elasticsearch.pv.ports.http.containerPort }}
      targetPort: {{ .Values.elasticsearch.pv.ports.http.containerPort }}
      protocol: TCP
      name: http
      nodePort: {{ .Values.elasticsearch.pv.ports.http.nodePort }}
    - port: {{ .Values.elasticsearch.pv.ports.transport.containerPort }}
      targetPort: {{ .Values.elasticsearch.pv.ports.transport.containerPort }}
      protocol: TCP
      name: transport
      nodePort: {{ .Values.elasticsearch.pv.ports.transport.nodePort }}
  selector:
    app: {{ .Values.elasticsearch.name }}
