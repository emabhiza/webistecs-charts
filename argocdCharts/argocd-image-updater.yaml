imageUpdater:
  enabled: true
  image:
    repository: quay.io/argoprojlabs/argocd-image-updater
    tag: latest
    pullPolicy: Always
  imagePullSecrets:
    - name: myregistrykey  # ✅ Add this under `imageUpdater`
data:
  accounts.image-updater: apiKey