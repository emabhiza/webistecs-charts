apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pricadata-test-pvc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage
  resources:
    requests:
      storage: 10Gi
  volumeName: pricadata-test-pv
