apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simple-deployment.fullname" . }}-test-connection"
  labels:
    {{- include "simple-deployment.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "simple-deployment.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
