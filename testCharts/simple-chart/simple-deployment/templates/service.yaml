apiVersion: v1
kind: Service
metadata:
  name: {{ include "simple-deployment.fullname" . }}
  labels:
    {{- include "simple-deployment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "simple-deployment.selectorLabels" . | nindent 4 }}
